# jarvEducation — Ανοιχτά Work Items

Σταθερά ids: το `#N` δίνεται μία φορά και δεν ξαναχρησιμοποιείται ποτέ, ούτε
αφού κλείσει το item. Το `Found:` είναι η ημερομηνία που **γράφτηκε** το item —
όχι προθεσμία.

Προτεραιότητες:

- **P1** — μπλοκάρει την επόμενη παράδοση.
- **P2** — πρέπει να γίνει πριν από την επόμενη παράδοση, αλλά δεν μπλοκάρει
  σήμερα.
- **P3** — επιθυμητό, μη προγραμματισμένο.

Το κλείσιμο ενός item το μετακινεί **αυτούσιο** στο `todo.archive.md` με
προσθήκη ημερομηνίας `Closed:`. Τίποτα δεν διαγράφεται ποτέ από αυτό το αρχείο,
και καμία εγγραφή δεν ξαναγράφεται στην έξοδο.

Αυτό το αρχείο καταγράφει *τι μένει*, όχι *γιατί*. Το σκεπτικό ζει στο
`docs/engineering-log.md`· οι αποφάσεις στο `docs/adr/`.

**Next id: #13**

## P1

_(κανένα — δεν υπάρχει προγραμματισμένη επόμενη παράδοση και τίποτα από τα
παρακάτω δεν μπλοκάρει τη ζωντανή εφαρμογή σήμερα)_

## P2

- **#1** — Οι Firestore security rules υπάρχουν **μόνο** στην κονσόλα του
  Firebase και πουθενά στο repo (το commit `a570d4f` το δηλώνει ρητά για τους
  κανόνες `players`/`results` delete και τα collections `signups`/`meta`).
  Είναι η μοναδική άμυνα δεδομένων παιδιών, αφού το web config είναι δημόσιο:
  χωρίς review, χωρίς ιστορικό, χωρίς τρόπο επαναφοράς αν χαλάσουν. Ελάχιστο
  ζητούμενο: αντίγραφο του τρέχοντος κειμένου των κανόνων μέσα στο repo, με
  ημερομηνία, ώστε να μπορεί να ξαναστηθεί. Found: 2026-09-07

- **#2** — Πρώτο test layer: μηχανικός έλεγχος των παραγόμενων
  `src/data/questions-{a..f}.json` ως προς το `Question` interface του
  `src/types.ts` — κάθε `options[]` string, `answer` μη κενό, μοναδικά ids,
  έγκυρα `category`/`type`. Το κενό είναι συγκεκριμένο και έχει ήδη χτυπήσει
  ζωντανά: στο `9d27618` δύο math generators έγραφαν αριθμούς αντί για strings
  και το quiz **πάγωνε** στο πάτημα απάντησης. Ο `vue-tsc` δεν βλέπει τίποτα
  εδώ (το `useGradeBanks.ts` κάνει `as Question[]` cast, ο generator είναι
  `.mjs`). Ο έλεγχος τρέχει πάνω στα πραγματικά αρχεία, όχι σε επανυλοποίηση
  των κανόνων του generator. Found: 2026-09-07

- **#3** — Γράψε μέσα στο repo τις ρυθμίσεις που ζουν εκτός version control και
  χωρίς τις οποίες δεν ξαναστήνεται το project: πηγή GitHub Pages = «GitHub
  Actions» και custom domain `quiz.jarvantage.com` στις ρυθμίσεις του repo (δεν
  υπάρχει αρχείο `CNAME`), CNAME → `jarva77.github.io` DNS-only στο Cloudflare,
  authorized domains και ενεργοποίηση Google sign-in στο Firebase. Υποχρέωση που
  επιβάλλει το ADR-0001. Found: 2026-09-07

## P3

- **#4** — Παλιά σχόλια στο `scripts/generate-questions.mjs`: η πρώτη γραμμή
  λέει ακόμα «Generates src/data/questions.json: 1000 questions for the Γ'
  Δημοτικού quiz» ενώ παράγει έξι αρχεία για έξι τάξεις, και το σχόλιο πάνω από
  το `gradePrefix` λέει «'c' (Γ') or 'd' (Δ')» ενώ τα προθέματα είναι πλέον
  `a`–`f`. Παραπλανά όποιον ανοίξει πρώτη φορά το script — που είναι το μοναδικό
  σημείο όπου γίνεται δουλειά περιεχομένου. Found: 2026-09-07

- **#5** — Το όριο των 50 θέσεων εγγραφής (`MAX_SIGNUP_SLOTS` στο
  `src/services/cloud.ts`) είναι client-only best-effort και **δεν υπάρχει
  ορατότητα**: πόσες θέσεις έχουν πιαστεί φαίνεται μόνο από την κονσόλα του
  Firebase, και όταν γεμίσει, ο νέος χρήστης απλώς αποσυνδέεται με μήνυμα. Να
  αποφασιστεί τι γίνεται όταν πλησιάσει το όριο (αύξηση + redeploy; ουρά;
  τίποτα;) πριν συμβεί, όχι μετά. Found: 2026-09-07

- **#6** — Δεν υπάρχει διαδικασία από την ανατροφοδότηση στη διόρθωση: το
  collection `feedback` (ratingSum/ratingCount/reportCount ανά ερώτηση)
  διαβάζεται χειροκίνητα από την κονσόλα του Firebase, και δεν υπάρχει
  καταγεγραμμένος τρόπος να γίνει μια αναφορά προβλήματος αλλαγή στον generator.
  Επιπλέον, τα ids των ερωτήσεων μετατοπίζονται όταν προστίθενται/αφαιρούνται
  ερωτήσεις (ADR-0002), οπότε παλιό feedback μπορεί να δείχνει σε άλλη ερώτηση.
  Found: 2026-09-07

- **#7** — Το `index.html` έχει μόνο `<title>`: κανένα `description`, κανένα
  `og:`/`twitter:` meta. Η εφαρμογή είναι ζωντανή και μοιράζεται με γονείς και
  συμμαθητές, οπότε ένας σύνδεσμος εμφανίζεται σαν γυμνό URL. Found: 2026-09-07

- **#8** — *Παρκαρισμένο, εγκεκριμένο για «αργότερα» — όχι προγραμματισμένο.*
  Κουμπί σίγασης ήχων (σήμερα οι ήχοι σωστού/λάθους παίζουν πάντα, δες
  `src/utils/sound.ts`). Found: 2026-09-07

- **#9** — *Παρκαρισμένο, εγκεκριμένο για «αργότερα» — όχι προγραμματισμένο.*
  Ένδειξη σερί σωστών απαντήσεων με 🔥. Found: 2026-09-07

- **#10** — *Παρκαρισμένο, εγκεκριμένο για «αργότερα» — όχι προγραμματισμένο.*
  PWA (manifest + service worker) και στη συνέχεια TWA για δημοσίευση στο Play
  Store (κόστος εγγραφής $25) για Android. Found: 2026-09-07

- **#11** — *Υπό συνθήκη — ενεργοποιείται μόνο αν συμβεί κάτι.* Anti-cheat /
  server-side scoring (σχέδιο: Cloudflare Worker) **μόνο** αν εμφανιστούν
  ύποπτα σκορ στη Βαθμολογία. Μέχρι τότε δεν γίνεται δουλειά εδώ· το item
  υπάρχει για να μη χρειαστεί να ξανασκεφτεί κανείς την απόφαση από την αρχή.
  Found: 2026-09-07

- **#12** — Ανοιχτό ερώτημα, χωρίς σχέδιο: το spec
  `docs/superpowers/specs/2026-09-05-games-platform-foundation-design.md`
  περιγράφει το PrimeQuiz (`games.jarvantage.com/primeQuiz`, ξεχωριστό repo) ως
  «τη μελλοντική νέα έκδοση του σημερινού εκπαιδευτικού quiz», ενώ ταυτόχρονα
  βάζει **ρητά εκτός scope** κάθε αλλαγή ή κατάργηση του `quiz.jarvantage.com`.
  Δεν υπάρχει απόφαση για το αν, πότε και πώς γίνεται μετάβαση, ούτε τι γίνεται
  με τα υπάρχοντα δεδομένα παικτών. Να αποφασιστεί ρητά πριν διαφοροποιηθεί
  αρκετά το ένα από το άλλο ώστε η μετάβαση να μη γίνεται πια. Found: 2026-09-07
